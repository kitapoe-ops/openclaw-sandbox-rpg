# ============================================
# scripts/.gitignore
# ============================================
# (Currently empty — all per-host state generated by these
# scripts is caught by the repo-root .gitignore. This file
# is kept as a place to add future per-host deployment
# artifacts that aren't broad enough to belong in the
# root .gitignore.)
